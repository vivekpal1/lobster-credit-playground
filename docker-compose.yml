version: "3.8"

services:
  ledger-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ledger-data:/app/data
    restart: unless-stopped

volumes:
  ledger-data:
